import 'package:flutter/material.dart';

class HealthProgressTrackingScreen extends StatelessWidget {
  const HealthProgressTrackingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Progress Screen"),
    );
  }
}
